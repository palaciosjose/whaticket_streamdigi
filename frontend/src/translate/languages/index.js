import { messages as englishMessages } from "./en";
import { messages as spanishMessages } from "../../pages/es";

const messages = {
        ...englishMessages,
        ...spanishMessages,
};

export { messages };
